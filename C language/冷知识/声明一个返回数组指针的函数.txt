typedef int a[10];

a b[10]; 为什么分配400个字节的空间？

int a[10];为什么分配了40个字节的空间？

问题：应该怎么解释typedef的这种行为呢？而如果换成是#define a int[10];以上定义则报错？求助！！！


typedef int a[10]这样声明之后，相当于数组类型a是int[10]这样的规格；那么a b[10]其实就相当于int b[10][10]，
10*10*sizeof(int)等于400个字节；而直接用#define a int[10]的话，相当于int[10] b[10]这样的语句是不符合语法规则的。