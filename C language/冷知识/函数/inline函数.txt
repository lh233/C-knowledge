调用函数时需要一定的时间开销和空间开销，这就要求要记下当时的执行的指令地址，还要保护“现场”。如果有的函数需要频繁使用，则使用的时间会很长。从而减低程序执行的时间。


C++或C提供了一种提高效率的方法，即在编译的时候将所用到的代码直接嵌入主函数中，而不是流程转出去，又称为内联函数。

指定函数的方法很简单，只需要在函数首行的左端加上一个关键字inline即可。

inline int max(int a, int b);
void main()
{
......

inline int max(int a, int b);
}

inline int max(int a, int b);