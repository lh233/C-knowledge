按位取反的话，不管是不是符号位都是0->1, 1->0;

>>、<<算术右移算术左移，运算规则：
算术右移：低位溢出，符号位不变，并用符号位补溢出的高位；
算术左移：符号位不变，低位补零

负数都是用补码进行运算，而且必须是先用它的补码；


在计算机中，整型类数据都是用补码来存储的；

为什么要用补码？
正数的原码表示简单，适用于乘除运算，但用于原码的表示的数进行加减法运算比较复杂，引入补码后，
减法可以当做加法来实现，且数的符号位也可以当做数值一样参与运算；
