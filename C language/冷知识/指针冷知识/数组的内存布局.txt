int a[5];


sizeof(a) = 20;			//表示sizeof(int) * 5
sizeof(a[0]) = 4;		
sizeof(&a) = 4;

/***************
sizeof(a[5])的值为4，虽然不存在a[5]这个元素，但是这里并没有真正的去访问a[5],而是仅仅根据数组元素的类型来确定
sizeof(a)为整个数组，因此是20，用于表示数组名，只有这里比较特殊；
****************/